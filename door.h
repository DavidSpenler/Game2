#ifndef DOOR
#define DOOR

#include <SFML/Graphics.hpp>
#include <SFML/Audio.hpp>
#include <vector>

/*using namespace std;
using namespace sf;

class Door {
	public:
		Door(Room*,Texture);
		set_key(int);
		set_destination(Room*);
		Sprite sprite;
		Texture image;
		FloatRect hit_box;
		Room* destination;
		Room* location;
		int key;
};*/

#endif
